# Sicherheits-, Hygiene- und Modulaudit

> **Stand:** 2026-03-18 | **Umfang:** Gesamtsystem (Frontend, Backend, Infrastruktur, Compliance-Docs)
> **Agents:** Waechter, Hygieniker, Modulwaechter, Konsistenzpruefer

---

## Zusammenfassung

| Bereich | Kritisch | Hoch | Mittel | Niedrig | Gesamt |
|---------|----------|------|--------|---------|--------|
| Sicherheit | 0 | 5 | 9 | 7 | 21 |
| Code-Hygiene | 0 | 2 | 9 | 5 | 16 |
| Modulstruktur | 0 | 0 | 0 | 1 | 1 |
| Konventionen | 0 | 0 | 2 | 4 | 6 |
| **Gesamt** | **0** | **7** | **20** | **17** | **44** |

**Gesamtbewertung:** Gut. Keine kritischen Schwachstellen. 7 hohe Befunde, davon 5 Security und 2 Code-Hygiene.

---

## 1. Sicherheits-Scan (Waechter)

### Hohe Befunde

| # | Befund | Datei | Empfehlung |
|---|--------|-------|------------|
| S-H1 | **Kein Account-Lockout** – Rate Limiting existiert (20/15min), aber kein Account-spezifischer Lockout. Brute-Force ueber verschiedene IPs moeglich. | `backend/routes/auth.js` | Account-Lockout nach 5 Fehlversuchen (15min Sperre). Zaehler in DB `login_attempts`. |
| S-H2 | **Fehlende Input-Validierung** – Keine systematische Schema-Validierung (Zod/Joi) fuer Request-Bodies. Verlaesst sich auf DB-Constraints. | Diverse Route-Handler | Zod-Schema-Validierung als Middleware fuer alle Public-Endpunkte. |
| S-H3 | **`dangerouslySetInnerHTML`** in E-Mail-Vorschau – Stored-XSS-Risiko bei kompromittiertem Admin-Account. | `EmailBrandingTab.tsx` | DOMPurify vor dem Rendering oder Vorschau in `<iframe sandbox>`. |
| S-H4 | **Logout: clearCookie ohne vollstaendige Cookie-Optionen** – Nur `{ path: '/' }` statt `httpOnly`, `secure`, `sameSite`. Browser loeschen den Cookie ggf. nicht zuverlaessig. | `backend/routes/auth.js:132-146` | `clearCookie('token', cookieOptions())` mit allen Attributen. |
| S-H5 | **Keine serverseitige Token-Revocation** – Logout loescht nur den Cookie clientseitig. Ein gestohlenes JWT bleibt 8h gueltig. | `backend/routes/auth.js` | Redis-Blacklist mit JWT-`jti` oder `invalidated_before` pro User. |

### Mittlere Befunde

| # | Befund | Empfehlung |
|---|--------|------------|
| S-M1 | SameSite=Lax statt Strict | Bewusste Designentscheidung, dokumentiert |
| S-M2 | Kein Passwort-Komplexitaets-Check (nur min. 8 Zeichen) | Min. 10 Zeichen + Grossbuchstabe + Zahl |
| S-M3 | Keine Request-ID/Correlation-ID in Logs | UUID-Middleware + Pino-Integration |
| S-M4 | Docker-Container laeuft als Root | `USER node` in Dockerfile.backend |
| S-M5 | PostgreSQL-Port exponiert in docker-compose.yml | Port-Mapping fuer Prod entfernen |
| S-M6 | Keine Dependency-Vulnerability-Checks in CI/CD | `npm audit` als CI-Job |
| S-M7 | CSRF nur via SameSite+CORS (bewusst akzeptiert) | Dokumentiert, akzeptables Restrisiko |
| S-M8 | SSW/BL Admin-Routes ohne Rate-Limiter – `/api/bl/admin` und `/api/ssw/admin` ohne Rate-Limit (nur Auth) | `rateLimiters.admin` beim Mount ergaenzen |
| S-M9 | CSV-Upload ohne fileFilter – Multer akzeptiert beliebige MIME-Typen fuer "CSV"-Import | `fileFilter` fuer `text/csv`, `text/plain`, Endung `.csv` |

### Niedrige Befunde

| # | Befund |
|---|--------|
| S-N1 | JWT-Secret als Env-Variable statt Docker Secret |
| S-N2 | Kein Refresh-Token (8h Lebensdauer) |
| S-N3 | Kein API-Versioning |
| S-N4 | `error.message` in einigen 500er-Responses |
| S-N5 | HSTS ohne preload/includeSubDomains |
| S-N6 | LIMIT als Template-Literal in dataSubject.js:522 (nicht nutzergesteuert, aber Style-Verstoss) |
| S-N7 | booking_requests SELECT mit LIMIT 3000 ohne Pagination (teacher.js:379) |

### Positiv-Befunde

- SQL-Injection: Alle Queries parametrisiert
- XSS: React-Escaping + CSP + E-Mail-Escaping
- Auth: JWT httpOnly Cookies, kein Bearer-Token
- CORS: Strikte Origin-Whitelist
- Rate Limiting: Auf allen relevanten Endpunkten
- File Upload: MIME-Check, Extension-Whitelist, SVG blockiert
- Security Headers: Helmet korrekt (CSP, X-Content-Type, X-Frame)
- RLS: Auf kritischen Tabellen aktiviert
- Audit Logging: PII-Zugriffe und Security-Events geloggt
- Passwort-Hashing: bcrypt, 10 Runden
- Keine Secrets in Versionskontrolle

---

## 2. Code-Hygiene (Hygieniker)

### Hohe Befunde

| # | Befund | Dateien | Empfehlung |
|---|--------|---------|------------|
| H-H1 | **Weekly-Schedule-Upsert dupliziert** – Nahezu identische Logik in 2 Dateien | `teacherRoutes.js`, `counselorAdminRoutes.js` | `shared/weeklyScheduleService.js` erstellen |
| H-H2 | **teacher.js 900+ Zeilen** – Appointments, Requests, Password, Slots, Feedback in einer Datei | `backend/routes/teacher.js` | Aufteilen in Sub-Module |

### Mittlere Befunde

| # | Befund | Empfehlung |
|---|--------|------------|
| H-M1 | Ungenutztes `teacherSystem` Import (`teacher.js:8`) | Import entfernen |
| H-M2 | Ungenutzte `buildHalfHourWindows` Funktion (`teacher.js:115`) | Funktion entfernen |
| H-M3 | Verwaiste `formatDate` in eventsRoutes.js | Entfernen |
| H-M4 | SSW `Category` und BL `Topic` duplizieren `CounselorTopic` | Lokale Interfaces durch zentralen Typ ersetzen |
| H-M5 | CalendarPanel-Logik teilweise in BLAdmin dupliziert | BLAdmin vollstaendig auf CalendarPanel migrieren |
| H-M6 | `teacherRoutes.js` 700+ Zeilen | CSV-Import und BL-Integration extrahieren |
| H-M7 | `AdminEvents.tsx` 600+ Zeilen | Sub-Komponenten extrahieren |
| H-M8 | `@types/node` in Frontend-package.json | Pruefen ob noetig |
| H-M9 | `.sidebarProfile__actionIcon--calendar` verwaist (gefixt) | CSS-Klasse wurde bereits entfernt |

### Niedrige Befunde

| # | Befund |
|---|--------|
| H-N1 | Auskommentierter ALTER TABLE Block in teacherRoutes.js |
| H-N2 | console.log in Seed-Scripts (akzeptabel) |
| H-N3 | Error-Handler-Pattern 24x wiederholt (Express-Error-Middleware) |
| H-N4 | cookie-parser Dependency – wird doch verwendet, kein Befund |
| H-N5 | Potentiell verwaiste Klassen in AdminDashboard.css |

---

## 3. Modulstruktur (Modulwaechter)

**Bewertung: Sehr gut**

| Kategorie | Status |
|-----------|--------|
| Struktur-Konsistenz | Alle 3 Module folgen dem Referenzmuster |
| Registry | Vollstaendig (3/3 Module) |
| Shared-Code-Nutzung | Vorbildlich (SSW/BL via Factories) |
| Migrations | Sauber nummeriert (001-040, naechste: 041) |
| Auth-Middleware | Korrekt auf allen Routes |
| Inter-Modul-Grenzen | Sauber getrennt, keine zirkulaeren Abhaengigkeiten |

Einziger Info-Befund: Elternsprechtag nutzt Core-Routes statt Module-Routes (historisch bedingt, funktional korrekt).

---

## 4. Konventions-Einhaltung (Konsistenzpruefer)

| Regel | Status |
|-------|--------|
| 1. ESM only | BESTANDEN |
| 2. SQL parametrisiert | BESTANDEN |
| 3. credentials: 'include' | BESTANDEN |
| 4. Array-Normalisierung vor .map() | 4x NIEDRIG (durch API-Client mitigiert) |
| 5. CSS-Variablen (keine hardcoded Farben) | BESTANDEN |
| 6. Keine Emojis in UI | BESTANDEN |
| 7. Typen in src/types/index.ts | 2x MITTEL (SSW Category, BL Topic lokal) |
| 8. API-Methoden in api.ts | BESTANDEN |
| 9. Auth Middleware | BESTANDEN |
| 10. Migrations (IF NOT EXISTS, TIMESTAMPTZ) | BESTANDEN |

8 von 10 Regeln vollstaendig eingehalten.

---

## Empfohlene Massnahmen-Reihenfolge

### Sofort (Quick Wins, < 1h)

1. **S-H4**: clearCookie mit vollstaendigen Cookie-Optionen (~5min)
2. **S-M8**: Rate-Limiter auf SSW/BL Admin-Routes (~5min)
3. **S-M9**: CSV-Upload fileFilter (~10min)
4. **H-M1/M2/M3**: Toten Code entfernen
5. **S-M4**: `USER node` in Dockerfile.backend
6. **H-M4**: Lokale Interfaces durch `CounselorTopic` ersetzen

### Kurzfristig (Sprint-faehig, < 1 Woche)

7. **S-H1**: Account-Lockout implementieren
8. **S-H3**: DOMPurify / iframe sandbox fuer EmailBrandingTab
9. **S-H5**: Serverseitige Token-Revocation (Redis-Blacklist oder DB-Feld)
10. **S-M2**: Passwort-Komplexitaet erhoehen
11. **H-H1**: Weekly-Schedule-Upsert dedup

### Mittelfristig (P2)

12. **S-H2**: Zod-Validierung fuer Public-Endpunkte
13. **H-H2**: teacher.js aufteilen
14. **S-M3**: Request-ID-Middleware
15. **S-M6**: npm audit in CI/CD

---

## Referenzen

| Dokument | Pfad |
|----------|------|
| Security Baseline | `docs/security/security-baseline.md` |
| DB-Audit (vorig) | `docs/security/db-audit-2026-03-17.md` |
| DSGVO-Anforderungen | `docs/compliance/dsgvo-anforderungen.md` |
| DSGVO SaaS-ToDo | `docs/compliance/dsgvo-saas-todo.md` |
