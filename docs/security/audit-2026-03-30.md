# Security-Audit-Bericht — 2026-03-30

> **Kontext:** Durchgeführt im Rahmen der Kysely-Migration + Security-Hardening (10-Tage-Plan).
> **Scope:** Vollständiges Backend, fokussiert auf SQL-Injection, Auth-Bypass, Credential-Exposure, RLS.
> **Tools:** Wächter-Agent (automatisiert), manuelle Code-Review.

## Zusammenfassung

| Schweregrad | Gefunden | Behoben | Offen |
|-------------|----------|---------|-------|
| Kritisch | 0 | 0 | 0 |
| Hoch | 7 | 5 | 2 (akzeptiert/dokumentiert) |
| Mittel | 1 | 1 | 0 |
| Niedrig | 0 | 0 | 0 |

## Behobene Befunde

| # | Kategorie | Befund | Fix |
|---|-----------|--------|-----|
| 1 | Credential-Leak | `counselorService.js` SELECT * leakte `calendar_token_hash` an Client | Explizite Spaltenliste ohne Token-Hash |
| 2 | Credential-Leak | Zweites SELECT * in `generateAppointmentSlots()` | Gleicher Fix |
| 3 | SQL-Injection (fragil) | `counselorAdminRoutes.js` DELETE IN mit Platzhalter-Interpolation | Kysely `.where('id', 'in', ids)` |
| 7 | Credential-Exposure | `public.js` SELECT * leakte `verification_token_hash` | Explizite Spaltenliste |
| 8 | JWT-Secret Schwäche | Fallback auf `SESSION_SECRET` wenn `JWT_SECRET` nicht gesetzt | Fallback entfernt |

## Akzeptierte Risiken

| # | Kategorie | Befund | Begründung |
|---|-----------|--------|------------|
| 4/5 | SQL-Injection (dynamisch) | `dataSubject.js` dynamische Spalten/WHERE in DSAR-Endpunkten | Hat Whitelist-Validierung (`ALLOWED_AUDIT_ACTIONS`, `ALLOWED_AUDIT_TABLES`). Vollständige Kysely-Migration als Backlog-Item. |
| 6 | Token-Revocation | Env-Admin (`ADMIN_USER`) Token kann nicht serverseitig widerrufen werden (kein DB-Eintrag) | Architektur-Limitation. Env-Admin hat 8h Token-Expiry. Kurzfristiger Fix: Token-Blacklist oder 1h Expiry. Als Backlog-Item dokumentiert. |

## Zusätzliche Security-Verbesserungen (proaktiv)

| Maßnahme | Status |
|----------|--------|
| `flow_aktivitaet` Retention-Cleanup (730 Tage) | Implementiert |
| Zombie-Tabellen `bl_topics`/`ssw_categories` deaktiviert | Implementiert (Migration 002) |
| `events` RLS Policy hinzugefügt (war ENABLE ohne Policy) | Implementiert (Migration 002) |
| `slots` ENABLE RLS ergänzt (hatte nur FORCE) | Implementiert (Migration 002) |
| Phone-Feld Regex-Validierung + 30-Char-Limit | Implementiert |
| npm audit: `brace-expansion` Vulnerability gefixt | Implementiert |
| Frontend + Backend: 0 npm vulnerabilities bestätigt | Geprüft |

## Nicht geprüft (Out of Scope)

- Penetration Testing (empfohlen vor Go-Live)
- OAuth-Provider-Konfiguration (Logineo, MS365)
- Docker-Container-Hardening
- TLS/SSL-Konfiguration auf VPS
