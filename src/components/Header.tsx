// Standard app header.
//
// Historical note: this component lived as `ExperimentalHeader`.
// We keep that export for backward compatibility, but new code should
// import and use `Header` from this module.

export { ExperimentalHeader as Header } from './ExperimentalHeader';
export type { ExperimentalHeaderProps as HeaderProps } from './ExperimentalHeader';
